import { createMuiTheme } from "@material-ui/core/styles";
export const theme = createMuiTheme({
  palette: {
    kanban: {
      main: "#fff",
      black: "#000",
      red: "#ff0000",
      orange: "#FFCE00",
      green: "#CCFFAA",
      blue: "#1976d2",
      focus: "#CCFFCC"
    }
  }
});
